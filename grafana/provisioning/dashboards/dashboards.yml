apiVersion: 1

providers:
  - name: spot-spread
    orgId: 1
    folder: Spot Spread
    folderUid: spot-spread
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
